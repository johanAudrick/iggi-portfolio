import Layout from "../../components/layout/layout";
import React from "react";

const Home = () => {
    const pageName = 'home';

    return (
        <>
            <Layout pageName={pageName}>
            </Layout>
        </>
    );
};

export default Home;